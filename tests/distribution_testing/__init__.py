"""These tests run syncs using the installed version of beetl, not using the source files.

This is to ensure that the distribution is working correctly after creating a build using `python -m build`."""
